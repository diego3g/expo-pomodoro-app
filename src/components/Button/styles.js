import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    color: "#FFF",
    fontFamily: "Poppins-Regular",
    fontSize: 16,
  },
  iconContainer: {
    marginLeft: 5,
  },
});
